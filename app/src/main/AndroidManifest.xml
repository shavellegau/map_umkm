<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Map_umkm">

        <activity android:name=".PesananActivity" />
        <activity android:name=".WishlistActivity" />
        <activity android:name=".UlasanActivity" />
        <activity android:name=".VoucherActivity" />
        <activity android:name=".PengaturanAkunActivity" />
        <activity android:name=".AlamatActivity" />
        <activity android:name=".BantuanActivity" />

        <!-- Admin Activity -->
        <activity android:name=".AdminActivity" />
        <activity android:name=".RegisterActivity" />

        <!-- Login Activity jadi launcher -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Kalau mau, MainActivity tetap ada tapi tanpa intent-filter -->
        <activity android:name=".MainActivity" />

    </application>
</manifest>
